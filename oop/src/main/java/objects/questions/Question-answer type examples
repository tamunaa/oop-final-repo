question-response: oneToOne
	QuestionResponse(String questionText, String correctAnswer);
	String getQuestion(); // question = What's the largest city in Georgia?
	int evaluate(String answer); // answer = Axalqalaqi
	String[] getChoices(); // null
	String[] getAnswers(); // {Axalqalaqi}
	1 point for the correct answer, 0 otherwise.

fill in the blank: OneToOne
	FillInTheBlank(String questionText, String correctAnswer);
	String getQuestion(); // question = _ is the largest city in Georgia.
	int evaluate(String answer); // answer = Axalqalaqi
    String[] getChoices(); // null
    String[] getAnswers(); // {Axalqalaqi}
	1 point for the correct answer, 0 otherwise.

picture-response: oneToOne
	PictureResponse(String URL, String correctAnswer);
	String getQuestion();	// question = URL
	int evaluate(String answer); // answer = Axalqalaqi
	String[] getChoices(); // null
	String[] getAnswers(); // {Axalqalaqi}
	1 point for the correct answer, 0 otherwise.

multiple choice: oneToMany (actual answer is one)
	MultipleChoice(String questionText, String[] choices, String answer);
	String getQuestion(); // question = The largest city in Georgia is:
	int evaluate(String answer); // answer = Axalqalaqi
	String[] getChoices(); // {Tbilisi,Qutaisi,Axalqalaqi,London}
	String[] getAnswers(); // {Axalqalaqi}
	1 point for the correct answer, 0 otherwise.

multiple choice with multiple answer: oneToMany
	MultipleChoiceWithMultipleAnswer(String questionText, String[] choices, String[] answers);
	String getQuestion(); // question = Axalqalaqi is the:
	int evaluate(String answers); // answers = largest city,most beautiful
    String[] getChoices(); // {largest city,smallest city,most beautiful city,most noisy city}
    String[] getAnswers(); // {largest city,most beautiful}
	1 point for each correct answer, 1 point penalty for each wrong answer.

matching: manyToMany
	Matching(String questionText, String[] questions, String[] choices);
	String getQuestion(); // Axalqalaqi,Tbilisi,Qutaisi;
	int evaluate(String answers); // answers = Qutaisi:I love this city,Tbilisi:has 'T' in its name,Qutaisi:I love this city
	String[] getChoices(); // {I love this city,is the most beautiful city,has 'T' in its name}  - answers are in a random order
	String[] getAnswers(); // {Qutaisi:I love this city,Tbilisi:has 'T' in its name,Qutaisi:I love this city}
	1 point for each correct answer, 0 otherwise.

multi-answer: oneToMany (several fields, can be ordered)
	MultiAnswer(String questionText, String answers[], int numFields, boolean isOrdered);
	String getQuestion(); // question = Name top 5 cities in Georgia
	int evaluate(String answers); // answer = Axalqalaqi,Tbilisi,Qutaisi,Batumi,London
	String[] getChoices(); // null
	String[] getAnswers(); // {Axalqalaqi}
	1 point for each correct answer, 0 otherwise. The answer is correct if both order (if it's ordered) and answer matches.

auto-generated: oneToOne
	AutoGenerated();
	String getQuestion(); // question = (5+3)*2 = ?
	int evaluate(String answer); // answer = "16"
	1 point for the correct answer, 0 otherwise.

graded questions: oneToOne
	GradedQuestion(String questionText);
	String getQuestion() // question = Some people think Axalqalaqi is cool. What do you think about that?
	int evaluate(String answer) // answer = Axalqalaqi is cool.